# Telegram listener (user account → read channel)
telethon>=1.34

# IQ Option API (NO oficial, comunidad). Instalar desde GitHub:
#   pip install "git+https://github.com/iqoptionapi/iqoptionapi.git"
# (la version de PyPI 'iqoptionapi' es un stub viejo sin stable_api). Trae websocket-client.
iqoptionapi @ git+https://github.com/iqoptionapi/iqoptionapi.git

# Config / env
python-dotenv>=1.0

# Telegram notify bot (UTF-8 sends via requests)
requests>=2.31

# IANA timezone database (REQUIRED on Windows: zoneinfo has no system tz DB)
tzdata>=2024.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
